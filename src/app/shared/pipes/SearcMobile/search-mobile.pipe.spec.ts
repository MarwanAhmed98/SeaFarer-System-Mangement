import { SearchMobilePipe } from './search-mobile.pipe';

describe('SearchMobilePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMobilePipe();
    expect(pipe).toBeTruthy();
  });
});
